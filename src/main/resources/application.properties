# Application name
spring.application.name=yocto-spring

# Server port
server.port=8080

# DataSource (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/yocto_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=Ming8524**
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# File upload size limit
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging (Hibernate SQL)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JSON ?? ?? snake_case ??
spring.jackson.property-naming-strategy=SNAKE_CASE

# URL ?? ?? ?? (???? PathPatternParser?? ??? ant ??? ??)
spring.mvc.pathmatch.matching-strategy=ant_path_matcher
